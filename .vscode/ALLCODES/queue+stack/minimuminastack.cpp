#include<iostream>
#include<stack>
using namespace std;
void getmin(stack<int> s ,int size){
    if(s.empty()){

    }
    
}
void push(int x){
    if(s.empty()){
        minEle = x;
        s.push(X);
        
    }

}
int main(){
    stack<int> s;
    int size,x;
    for(int i=0;i<size;i++){
        cin>>x;
        push(x);
    }

    getmin(s,size);
}